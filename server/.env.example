# MongoDB connection string (local or Atlas)
MONGO_URI=mongodb://localhost:27017/ledgermatch

# JWT signing secret — MUST be set in production.
# If omitted, the server falls back to a hardcoded key which is fine for local dev
# but completely insecure in any deployed environment.
JWT_SECRET=replace-with-a-strong-random-secret
